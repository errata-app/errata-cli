# Ship Skill
1. Run `go test ./...`, `go vet ./...`, and `golangci-lint run ./...`
2. Stage all changes with `git add -A`
3. Create a commit with a conventional commit message
4. Push to the current branch with `git push`
5. Output a PR description as plain text (do NOT use gh CLI)
