# Fresh Branch Skill
1. Run `git checkout main && git pull` to get latest main
2. Create a new branch with `git checkout -b <branch-name>`
3. Branch name should use conventional prefixes: `feature/`, `fix/`, `chore/`, `refactor/`, `test/`, `docs/`
4. Ask the user for a branch name if not provided
